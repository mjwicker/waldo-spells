# Cycle 8 — 2026-04-25 12:00

## Task Type
benchmark

## Cycle Focus
Task 4 — run full Fast-tier benchmark on all 32 runnable corpus items, generate results.csv + summary.md, write harness/results/analysis.md (decision artifact closing the research sprint).

## Artifact
harness/results/analysis.md — complete

Also produced:
- harness/results/results.csv (32 rows, Fast tier)
- harness/results/summary.md (overall + by-tier + by-input-type tables)

## Summary
Fast tier (pyenchant/hunspell): p50=0.93ms, p99=2.75ms — passes the 200ms bar with ~200× headroom. Precision=0.750, Recall=0.333, F1=0.462. 18 false negatives categorized: 56% homophones (affect/effect, their/there, you're/your), 28% complex grammar (would of, pronoun case), 17% spelling-harness artifacts. Primary FP driver: `async` not in en_US dictionary — requires context_hint=code from content script for GitHub inputs. ai_chat scores 0/0/0/0: correct behavior — all test items used homophones, which spell-check cannot detect by design.

## Gaps / Open Questions
- Mid tier (gec-t5_small / CTranslate2 INT8): model not downloaded; benchmark pending CT2_MODEL_PATH
- Smart tier (Qwen2.5-3B Q4_K_M): model not downloaded; benchmark pending LLAMA_MODEL_PATH
- FP rate (0.273) cannot validate 6-sigma target on 32 items — documented as caveat in analysis.md
- async/technical-jargon FP fix requires content script context detection from Task 3

## Strategic Close

CYCLE ASSESSMENT: Decisive. The benchmark ran cleanly end-to-end, the FN categorization is complete and honest, and the analysis.md provides a usable decision artifact. The central finding (ai_chat 0/0/0/0) is clearly stated: Fast tier is a necessary pre-filter, not a sufficient product. The analysis is production-quality and does not oversell Fast tier performance.

NEXT CYCLE FOCUS: Research sprint is complete. Next phase is v0.2.0 — Firefox extension wiring. Specific work: (1) manifest.json + content.js skeleton watching `<textarea>` and `<input type="text">`; (2) context detection implementing `context_hint=code` for GitHub/code inputs per browser_context_detection_research.md findings; (3) localhost:8000 call to wrapper/main.py Fast tier; (4) inline highlight DOM overlay. This is build-type work. Haiku eligible for new isolated files (manifest.json, content.js); Sonnet for wiring and integration tests.

HAIKU ELIGIBLE: yes for new isolated extension files (manifest.json, content.js, background.js); no for integration with existing wrapper/ code.

RESEARCH HEALTH: Complete. All 4 research tasks are ✅. The research sprint is closed. v0.2.0 development can begin.
