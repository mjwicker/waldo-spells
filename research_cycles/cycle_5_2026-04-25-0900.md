# Cycle 5 — 2026-04-25 09:00

## Task Type
build

## Cycle Focus
Task 2, sub-unit 3b — wire Qwen2.5-3B-Instruct (Smart tier) into wrapper/llama_backend.py with full llama-server subprocess management.

## Artifact
GrammarChecker/wrapper/llama_backend.py (rewritten, ~170 lines)
GrammarChecker/wrapper/tests/test_llama_backend.py (new, 6 tests)

## Summary
llama_backend.py rewritten from stub to full implementation: lazy llama-server startup (subprocess.Popen), /health polling (60s timeout), POST /v1/chat/completions with JSON-schema-constrained output, offset/length validation before returning Correction objects, atexit cleanup. is_available() checks LLAMA_MODEL_PATH env var + file exists + llama-server binary in PATH or LLAMA_SERVER_BIN. All stub behavior is preserved when model is absent — correct() raises RuntimeError with actionable install instructions. 16/16 tests pass. No model download required this cycle.

## Gaps / Open Questions
- Smart tier untested end-to-end (requires model + llama-server on disk — user action needed)
- t5_backend.py (Mid/Better tier) still a stub — needs CTranslate2 wiring (Cycle 6)
- Expected latency on N6000: 30–90s/sentence (CPU-only) — will be confirmed by Task 4 harness

## Strategic Close

CYCLE ASSESSMENT: Strong build cycle. llama_backend.py is production-ready modulo actual model availability. The implementation is defensive: any missing precondition is logged with a specific message, and the rest of the system continues working on Fast tier. The JSON output parsing validates offsets against the original text, so malformed model output can't crash the router.

NEXT CYCLE FOCUS: Cycle 6 — Task 2 sub-unit 3d: wire CTranslate2 + gec-t5_small into t5_backend.py (Mid/Better tier). Steps: (1) implement is_available() checking for ctranslate2 package + model directory at CT2_MODEL_PATH env var, (2) implement correct() loading ctranslate2.Translator, tokenizing with transformers T5Tokenizer, running translate_batch(), mapping output back to Correction objects, (3) add t5_converter.py helper script for one-time HuggingFace → CT2 conversion. No model download this cycle.

HAIKU ELIGIBLE: no — t5_backend.py is an existing shared file; Sonnet implements directly.

RESEARCH HEALTH: On track. Task 2 is now 3a ✅ (Fast), 3b ✅ (Smart wired), 3c ✅ (T5 status doc), 3d ⚪ (Mid tier wiring). After Cycle 6 completes 3d, Task 2 closes and Task 4 (instrumented harness) begins. All three tiers will be wirable at that point.
