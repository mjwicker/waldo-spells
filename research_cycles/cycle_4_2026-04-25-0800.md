# Cycle 4 — 2026-04-25 08:00

## Task Type
investigate

## Cycle Focus
Task 2, sub-unit 3c — T5 GGUF support investigation: can Unbabel/gec-t5_small be served via llama.cpp, or do we need an alternative runtime for the Mid tier?

## Artifact
GrammarChecker/research/t5_gguf_status.md (443 lines, 8 sections)

## Summary
llama.cpp T5 encoder-decoder support is production-ready (PR #8141 merged July 2024, conversion via PR #8055), but lacks llama-server HTTP API support — only llama-cli works. No pre-converted GGUF for gec-t5_small exists; conversion is possible but the wrapper integration would require a custom C++ bridge. CTranslate2 emerges as the better choice for Mid tier: INT8 quantization is mature, 6–10s/sentence on N6000, ~120 MB footprint, and Python integration fits the existing wrapper architecture cleanly. PyTorch+int8 ruled out (too heavy, ~350–480 MB). Also fixed a pre-assignment bug in wrapper/main.py (request = None before try block).

## Gaps / Open Questions
- Quantization accuracy unverified: does INT8 preserve F₀.₅ within 2%? Needs actual benchmark.
- Real N6000 latency unmeasured — estimates are 6–10s/sentence but unconfirmed.
- CTranslate2 vs llama.cpp side-by-side profiling deferred to Task 4 harness.

## Strategic Close

CYCLE ASSESSMENT: Decisive advance. The T5 runtime question is answered: CTranslate2 is the correct choice for Mid tier. The artifact is decision-quality — actionable for Cycle 5 wiring. The main.py bug fix removes a latent error path. One open question (quantization accuracy) is appropriate to defer to the benchmark harness in Task 4.

NEXT CYCLE FOCUS: Cycle 5 — Task 2 sub-unit 3b: wire Qwen2.5-3B-Instruct GGUF into llama_backend.py. Specific steps: (1) download Qwen2.5-3B-Instruct-Q4_K_M.gguf via huggingface-cli or wget, (2) update llama_backend.py is_available() to check LLAMA_MODEL_PATH env var + file exists, (3) implement correct() by spawning llama-server subprocess and calling /v1/chat/completions, (4) define JSON-schema-constrained output format for grammar corrections. No model downloads this session — verify path, then download as a one-time setup step before wiring.

HAIKU ELIGIBLE: no for next cycle — llama_backend.py is an existing file that needs non-trivial changes; Sonnet should implement directly.

RESEARCH HEALTH: On track. Tasks 1 (capability matrix) and 3 (context detection) complete. Task 2 wrapper is 3a (scaffold) and 3c (T5 status) done; 3b (Qwen wiring) is the next sub-unit. Task 4 (harness) can begin scaffolding in parallel with 3b since Fast tier protocol is stable.
