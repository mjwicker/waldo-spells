# Cycle 17 — waldo-spells — 2026-05-29 14:29

## Project
waldo-spells

## Test Results
- Passed: 126 / 126
- Failed: 0
- Unresolved: none

## Changes
**Files changed:**
- `harness/onnx_backend.py` (new) — ONNX edge tier classification runner with zero-dependency WordPiece tokenizer
- `harness/runner.py` — wired edge tier via `_run_edge()` helper; tier_not_applicable for non-tone tasks
- `harness/test_onnx_backend.py` (new) — 23 tests covering is_available(), classify_tone(), tokenizer, benchmark_tone(), integration paths
- `wrapper/requirements.txt` — added onnxruntime
- `pyproject.toml` — locked version deps
- `uv.lock` — updated dependency lock
- `research_cycles/cycle_17_2026-05-29.md` (new) — this report
- `models/distilbert-sst2-onnx/` (new directory) — DistilBERT SST-2 INT8 ONNX model + config
- `models/all-MiniLM-L6-v2-onnx/` (new directory) — all-MiniLM-L6-v2 INT8 ONNX model + config

**Summary:**
Downloaded two ONNX classification models from HuggingFace (Xenova/): DistilBERT SST-2 INT8 (~64MB) and all-MiniLM-L6-v2 INT8 (~22MB). Implemented zero-dependency WordPiece tokenizer in `onnx_backend.py` (no sentencepiece/transformers dep). Wired as edge tier in `runner.py` with tier_not_applicable filter for non-tone tasks. Benchmarked on all 3,000 UCI sentiment items: 92.87% accuracy, 52.7ms mean latency, 98.3ms P95 latency on i5-8400 CPU-only.

## Issues
None. All 126 tests passing; clean benchmark run with no regressions.

## Next cycle hint
Consider T-SPELLS-EDGE-2 (Wire Edge tier model into v0.4.0 extension) — bundle winning ONNX model into Firefox extension via Transformers.js v4. Gate: T-SPELLS-EDGE-1 now shows acceptable tone_accuracy (92.87%). Project: waldo-spells. Haiku eligible: no (requires DOM/extension wiring).
